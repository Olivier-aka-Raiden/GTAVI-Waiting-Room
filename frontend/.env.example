# Frontend environment variables
# Copy to .env.local for local dev (gitignored)
# Set the same vars in Vercel dashboard for production

# Only needed if NOT using Vercel rewrites (vercel.json)
# VITE_API_BASE_URL=https://gtavi-api-xxxxxxxxxx-ew.a.run.app
